<Window x:Class="WpfLab3.Views.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource AppWindowStyle}"
        Title="{Binding WindowTitle}"
        Height="420" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding WindowTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Название" Margin="0,0,0,4"
                   Foreground="{StaticResource MutedTextBrush}"/>
        <TextBox Grid.Row="2"
                 Style="{StaticResource AppTextBoxStyle}"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                 MaxLength="100"
                 Margin="0,0,0,12"/>

        <TextBlock Grid.Row="3" Text="Описание" Margin="0,0,0,4"
                   Foreground="{StaticResource MutedTextBrush}"/>
        <TextBox Grid.Row="4"
                 Style="{StaticResource AppTextBoxStyle}"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                 MaxLength="500"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 VerticalContentAlignment="Top"
                 Height="110"/>

        <CheckBox Grid.Row="5" Content="Выполнена"
                  IsChecked="{Binding IsCompleted}"
                  Margin="0,16,0,16"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Style="{StaticResource GhostButtonStyle}"
                    Command="{Binding CancelCommand}" Margin="0,0,8,0" MinWidth="100"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding SaveCommand}" MinWidth="120" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
